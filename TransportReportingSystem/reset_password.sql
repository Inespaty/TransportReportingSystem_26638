-- Password reset script
-- Use environment variables or secure methods to set passwords
-- Example: UPDATE app_user SET password = ? WHERE email = ?;
-- Never hardcode passwords in SQL files

-- For testing, use proper password reset flow through the application

